<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android" android:layout_marginTop="20dp"
    android:layout_width="match_parent" android:layout_height="80dp" android:background="@drawable/custom_border"
    android:layout_marginBottom="50px"
    android:layout_marginRight="80px"
    android:layout_marginLeft="80px"
    >

    <EditText
        android:id="@+id/editText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textAppearance="?android:attr/textAppearanceLarge"
        android:layout_alignParentEnd="true"
        android:layout_alignParentTop="true"
        android:layout_alignParentBottom="true"
        android:gravity="left|center"
        android:layout_alignParentStart="true"
        android:paddingBottom="10dp"
        android:textColor="@color/colorTextItemBlue"
        android:background="@color/colorLightBlue"
        android:singleLine="true"
        android:imeOptions="actionDone"/>

    <ImageButton
        android:id="@+id/editButton"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:text="Edit"
        android:layout_centerVertical="true"
        android:layout_alignParentEnd="true"
        android:src="@drawable/edit_button"
        android:scaleType="fitXY"
        android:background="@null"/>

</RelativeLayout>